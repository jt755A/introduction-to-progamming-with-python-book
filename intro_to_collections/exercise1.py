# If you have a list named people, how can you determine the number of entries in that list?

# use a len function for that list: list: len(people) = ordered collection.
my_list = [1, 7, 'ham', 17, [4, 8, 9]]
print(len(my_list))