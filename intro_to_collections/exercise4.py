# Why can we treat strings as sequences?
# When reference is made to individual characters, strings are effectively sequences.
# They are iterable objects when passed to other constructor functions

# EDIT: they are ordered; you can access individual characters with indexing.